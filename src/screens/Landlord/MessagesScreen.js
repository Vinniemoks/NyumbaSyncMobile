import React from 'react';
import MessagesScreen from '../Shared/MessagesScreen';

const LandlordMessagesScreen = ({ navigation }) => {
  return <MessagesScreen navigation={navigation} />;
};

export default LandlordMessagesScreen;
